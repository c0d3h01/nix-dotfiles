#!/bin/sh

# Show commit count in the current repository.
if command -v tput >/dev/null 2>&1; then
	GREEN="$(tput setaf 2)"
	RESET="$(tput sgr0)"
else
	GREEN=""
	RESET=""
fi

printf '★ %s%s%s commits so far\n' "$GREEN" "$(git rev-list --count HEAD)" "$RESET"
